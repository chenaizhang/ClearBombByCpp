#!/usr/bin/env bash
set -euo pipefail

# TODO: Replace with actual build + run pipeline once backend libs are selected.

echo "TODO: Configure backend build steps (cmake, ninja, etc.) before running server." >&2
